[V4Z_CANONICALISE_FRAGMENT_CORE_FLAGS_AND_BASE_PATH_DOMINANCE]
repo_root=C:\Users\wjvalo\OneDrive - Queensland Treasury\Documents\UQ\7013\proj\New folder\Fragments_PDPTWEV-Q
fragment_path=C:\Users\wjvalo\OneDrive - Queensland Treasury\Documents\UQ\7013\proj\New folder\Fragments_PDPTWEV-Q\evrp_fragments\fragment_core.py
audit_only=False
before_fragment_core_ast_parse_ok=True
after_fragment_core_ast_parse_ok=True
dominance_block_replaced_lines=530-717
header_changed=True
stale_globals_removed=0
has_canonical_state_key=True
has_canonical_metrics=True
has_canonical_prune=True
has_print_alias=True
has_local_newwork_hook=True
step_dominance_removed=True
remaining_stale_symbol_count=0
fragment_core_changed=True
fragment_core_diff_line_count=272
fragment_core_diff_preview:
--- fragment_core.py.before
+++ fragment_core.py.after
@@ -1,6 +1,7 @@
-"""Mechanically extracted fragment layer
-Generated by experiments.build_consolidated_v1a_fragments.
-Source module is evrp_fragments_consolidated_v1/legacy_core.py.
+"""Fragment generation and dominance-filtering layer.
+
+Originally mechanically extracted from the consolidated legacy module. This module
+is now maintained directly as the active fragment-generation implementation.
 """
 
 from __future__ import annotations
@@ -527,162 +528,66 @@
     return (path + (j,), phase2, frozenset(onboard2), E_arr, t_depart2,
             frozenset(seenP2), frozenset(seenD2), seenS, deliv2,dist2), None
 
-# === BASE_PATH_WORKING_SET_DOMINANCE_PRUNING_APPLIED: start ===
-# Conservative base-path dominance pruning.
-#  Step-level dominance is disabled by default after over-pruning:
-#   (path, phase, onboard, energy, time, seenP, seenD, seenS, deliv, distance)
+# === BASE_PATH_WORKING_SET_DOMINANCE: start ===
+# Canonical local base-path dominance pruning.
+#
+# Active state tuple:
+#   (path, phase, onboard, E, t_depart, seenP, seenD, seenS, deliv_count, distance)
+#
+# The pruning hook is intentionally layer-local: enumerate_base_paths first builds the
+# full NewWork layer, then prunes dominated states immediately before Work = NewWork.
 BASE_PATH_WORKING_SET_DOMINANCE_ENABLED = os.getenv(
     "EVRP_BASE_PATH_WORKING_SET_DOMINANCE", "1"
 ).strip().lower() not in {"0", "false", "no", "off"}
-
-# def _bp_tuple_field(state, index, default=None):
-#     try:
-#         return state[index]
-#     except Exception:
-#         return default
-#
-#
-# def _bp_state_get(state, *names, default=None):
-#     for name in names:
-#         if isinstance(state, dict) and name in state:
-#             return state[name]
-#         if hasattr(state, name):
-#             return getattr(state, name)
-#     tuple_name_to_index = {
-#         "seq": 0, "path": 0, "pi": 0,
-#         "phase": 1, "phi": 1,
-#         "onboard": 2, "load_set": 2, "Lset": 2,
-#         "E": 3, "energy": 3, "battery": 3, "remaining_energy": 3,
-#         "tau": 4, "time": 4, "t": 4,
-#         "picked": 5, "SP": 5, "seen_pickups": 5,
-#         "delivered": 6, "SD": 6, "seen_deliveries": 6,
-#         "seen_stations": 7, "SS": 7,
-#         "deliv": 8,
-#         "d": 9, "dist": 9, "distance": 9, "cost": 9,
-#     }
-#     if isinstance(state, tuple):
-#         for name in names:
-#             if name in tuple_name_to_index:
-#                 return _bp_tuple_field(state, tuple_name_to_index[name], default)
-#     return default
-#
-#
-# def _bp_normalise_set(value):
-#     if value is None:
-#         return frozenset()
-#     if isinstance(value, frozenset):
-#         return value
-#     if isinstance(value, set):
-#         return frozenset(value)
-#     if isinstance(value, (list, tuple)):
-#         return frozenset(value)
-#     return frozenset([value])
-#
-#
-# def _bp_state_seq(state):
-#     seq = _bp_state_get(state, "seq", "path", "pi", default=None)
-#     if seq is None:
-#         return None
-#     try:
-#         return tuple(seq)
-#     except TypeError:
-#         return None
-
-
-def _bp_customer_skeleton(data, seq):
-    if seq is None:
-        return None
-    try:
-        return tuple(sid for sid in seq if is_customer(data, sid))
-    except Exception:
-        return None
-
-#
-# def _bp_current_sid(state, seq):
-#     cur = _bp_state_get(state, "current", "cur", "last", "last_sid", "sid", "node", default=None)
-#     if cur is not None:
-#         return cur
-#     if seq:
-#         return seq[-1]
-#     return None
+BASE_PATH_DOMINANCE_EPS = 1e-9
+
+
+def _base_path_customer_skeleton(data, path):
+    return tuple(sid for sid in path if is_customer(data, sid))
+
+
 def _base_path_state_key(data, state):
+    """Structural key for local dominance within one base-path depth layer."""
     path, phase, onboard, E, t_depart, seenP, seenD, seenS, deliv_count, distance = state
-    customer_skeleton = tuple(sid for sid in path if is_customer(data, sid))
-    current_node = path[-1]
     return (
-        customer_skeleton,
-        current_node,
+        _base_path_customer_skeleton(data, path),
+        path[-1],
         phase,
         onboard,
         seenP,
         seenD,
     )
 
+
 def _base_path_state_metrics(state):
+    """Comparable metrics for local dominance; lower tuple values are better."""
     path, phase, onboard, E, t_depart, seenP, seenD, seenS, deliv_count, distance = state
-    return (t_depart, distance, -E, len(onboard))
-
-def _dominates_base_path_state(a, b):
-    return (
-        a.t_depart <= b.t_depart + EPS
-        and a.distance <= b.distance + EPS
-        and a.E >= b.E - EPS
-        and len(a.onboard) <= len(b.onboard) + EPS
-    )
-
-def _bp_metric_tuple(state):
-    def as_float_or_none(value):
-        if value is None:
-            return None
-        try:
-            return float(value)
-        except Exception:
-            return None
-    t = as_float_or_none(_bp_state_get(state, "tau", "time", "t", default=None))
-    d = as_float_or_none(_bp_state_get(state, "d", "dist", "distance", "cost", default=None))
-    e = as_float_or_none(_bp_state_get(state, "E", "energy", "battery", "remaining_energy", default=None))
-    load = _bp_normalise_set(_bp_state_get(state, "onboard", "load_set", "Lset", default=None))
-    return t, d, None if e is None else -e, len(load)
-
-
-def _bp_metric_dominates(a_metrics, b_metrics, eps=1e-9):
-    any_common = False
-    strictly_better = False
-    for av, bv in zip(a_metrics, b_metrics):
-        if av is None or bv is None:
-            if av is not None or bv is not None:
-                return False
-            continue
-        any_common = True
-        if av > bv + eps:
-            return False
-        if av < bv - eps:
-            strictly_better = True
-    return any_common and strictly_better
-
-
-def _bp_state_dominance_key(data, state, ordinal=None):
-    seq = _bp_state_seq(state)
-    customer_skeleton = _bp_customer_skeleton(data, seq)
-    cur = _bp_current_sid(state, seq)
-    if customer_skeleton is None or cur is None:
-        return ("uninterpretable", ordinal if ordinal is not None else id(state))
-    onboard = _bp_normalise_set(_bp_state_get(state, "onboard", "load_set", "Lset", default=None))
-    picked = _bp_normalise_set(_bp_state_get(state, "picked", "SP", "seen_pickups", default=None))
-    delivered = _bp_normalise_set(_bp_state_get(state, "delivered", "SD", "seen_deliveries", default=None))
-    phase = _bp_state_get(state, "phase", "phi", default=None)
-    # Keep full customer skeleton in the key for exactness: this only collapses
-    # inferior station/timing variants of the same partial customer order.
-    return (customer_skeleton, cur, phase, onboard, picked, delivered)
+    return (float(t_depart), float(distance), -float(E), len(onboard))
+
+
+def _base_path_metrics_dominate(a_metrics, b_metrics, eps=BASE_PATH_DOMINANCE_EPS):
+    """Return True if a is no worse than b and strictly better in at least one metric."""
+    no_worse = all(a <= b + eps for a, b in zip(a_metrics, b_metrics))
+    strictly_better = any(a < b - eps for a, b in zip(a_metrics, b_metrics))
+    return no_worse and strictly_better
 
 
 def _prune_base_path_working_set(data, work_states):
+    """Prune dominated states from a completed NewWork layer."""
     if not BASE_PATH_WORKING_SET_DOMINANCE_ENABLED:
-        return work_states, {"enabled": False, "input": len(work_states), "output": len(work_states), "dominated_removed": 0, "keys": 0, "max_bucket_size": 0}
+        return work_states, {
+            "enabled": False,
+            "input": len(work_states),
+            "output": len(work_states),
+            "dominated_removed": 0,
+            "keys": 0,
+            "max_bucket_size": 0,
+        }
+
     buckets = {}
-    for ordinal, state in enumerate(work_states):
-        key = _bp_state_dominance_key(data, state, ordinal)
-        buckets.setdefault(key, []).append(state)
+    for state in work_states:
+        buckets.setdefault(_base_path_state_key(data, state), []).append(state)
+
     kept = []
     dominated_removed = 0
     max_bucket_size = 0
@@ -690,23 +595,35 @@
         max_bucket_size = max(max_bucket_size, len(group))
         nondominated = []
         for candidate in group:
-            cand_metrics = _bp_metric_tuple(candidate)
-            if any(_bp_metric_dominates(_bp_metric_tuple(existing), cand_metrics) for existing in nondominated):
+            cand_metrics = _base_path_state_metrics(candidate)
+            if any(
+                _base_path_metrics_dominate(_base_path_state_metrics(existing), cand_metrics)
+                for existing in nondominated
+            ):
                 dominated_removed += 1
                 continue
+
             survivors = []
             for existing in nondominated:
-                if _bp_metric_dominates(cand_metrics, _bp_metric_tuple(existing)):
+                if _base_path_metrics_dominate(cand_metrics, _base_path_state_metrics(existing)):
                     dominated_removed += 1
                     continue
                 survivors.append(existing)
             survivors.append(candidate)
             nondominated = survivors
         kept.extend(nondominated)
-    return kept, {"enabled": True, "input": len(work_states), "output": len(kept), "dominated_removed": dominated_removed, "keys": len(buckets), "max_bucket_size": max_bucket_size}
-
-
-def _bp_print_dominance_event(depth, before, after, removed, keys=0, max_bucket_size=0, source="work"):
+
+    return kept, {
+        "enabled": True,
+        "input": len(work_states),
+        "output": len(kept),
+        "dominated_removed": dominated_removed,
+        "keys": len(buckets),
+        "max_bucket_size": max_bucket_size,
+    }
+
+
+def _print_base_path_dominance_event(depth, before, after, removed, keys=0, max_bucket_size=0, source="local_newwork"):
     print(
         "[BASE-PATH-DOMINANCE] "
         f"source={source} depth={depth} before={before} after={after} "
... diff preview truncated; total_diff_lines=272
fragment_core_backup_created=C:\Users\wjvalo\OneDrive - Queensland Treasury\Documents\UQ\7013\proj\New folder\Fragments_PDPTWEV-Q\evrp_fragments\fragment_core.py.bak_v4z_canonicalise_fragment_core_flags_and_base_path_dominance_20260608_011215
fragment_core_patch_written=True
patch_written=True
scope_note=Canonicalised base-path dominance helpers and removed known stale globals. Local NewWork pruning is preserved.
next_step=run c103C16 benchmark and c103C16_2 fragment profile as smoke checks
